# NetGuardian - Network Analysis Suite Requirements
# Install with: pip install -r requirements.txt

# Core packet manipulation library
scapy>=2.4.5

# Python interface to nmap
python-nmap>=0.7.1

# GUI framework (for graphical interface)
PyQt6>=6.5

# Service discovery and network enumeration
zeroconf>=0.47.1
bleak>=0.19.5
psutil>=5.9.0
python-nmap>=0.7.1
# Vendor lookup for MAC addresses
manuf>=1.1.1

# Network topology visualization
networkx>=3.0
matplotlib>=3.6.0

# DNS reconnaissance
dnspython>=2.3.0

# CVE database integration
requests>=2.28.0

# Web application scanning (future enhancement)
# python-wappalyzer>=0.3.1
# beautifulsoup4>=4.11.0

# Standard library modules (no installation needed):
# - argparse (for command-line interface)
# - logging (for logging functionality)  
# - ipaddress (for IP address validation)
# - socket (for network operations)
# - subprocess (for running external commands)
# - threading (for concurrent operations)
# - time (for timing operations)
# - typing (for type hints)
# - sys, os, re (standard utilities)
